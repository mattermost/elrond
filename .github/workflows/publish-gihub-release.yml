---
on:
  push:
    tags:
    - v[0-9]+(\.[0-9]+)*

env:
  GH_TOKEN: "${{ secrets.GH_TOKEN }}"

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: make deps
    - run: make release
